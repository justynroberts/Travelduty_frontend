apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: travelduty
spec:
  replicas: 3
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
      - name: redis
        image: travelduty/redis:4.0.8
        ports:
        - containerPort: 3809
        resources:
          requests:
            memory: "128Mi"
            cpu: "250m"
