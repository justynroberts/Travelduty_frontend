apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: travelduty
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
      - name: redis
        image: travelduty/redis:1.4.8
        ports:
        - containerPort: 3736
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
