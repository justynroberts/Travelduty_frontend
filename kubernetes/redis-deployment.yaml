apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: travelduty
spec:
  replicas: 2
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
      - name: redis
        image: travelduty/redis:1.9.7
        ports:
        - containerPort: 7430
        resources:
          requests:
            memory: "128Mi"
            cpu: "500m"
