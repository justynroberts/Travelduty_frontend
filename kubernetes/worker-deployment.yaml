apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: travelduty
spec:
  replicas: 2
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
      - name: worker
        image: travelduty/worker:1.9.1
        ports:
        - containerPort: 4289
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
