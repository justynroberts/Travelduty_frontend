apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: travelduty
spec:
  replicas: 4
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
      - name: worker
        image: travelduty/worker:4.4.0
        ports:
        - containerPort: 6069
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
