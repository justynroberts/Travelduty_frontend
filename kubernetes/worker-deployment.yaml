apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: travelduty
spec:
  replicas: 5
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
      - name: worker
        image: travelduty/worker:2.3.7
        ports:
        - containerPort: 3666
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
