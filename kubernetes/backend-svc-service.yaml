apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: travelduty
spec:
  type: LoadBalancer
  ports:
  - port: 8525
    targetPort: 8811
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: backend
