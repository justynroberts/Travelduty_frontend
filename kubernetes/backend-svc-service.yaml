apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: travelduty
spec:
  type: NodePort
  ports:
  - port: 8970
    targetPort: 8419
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: backend
