apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: travelduty
spec:
  type: LoadBalancer
  ports:
  - port: 8584
    targetPort: 8640
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: backend
