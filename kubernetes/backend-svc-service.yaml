apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: travelduty
spec:
  type: LoadBalancer
  ports:
  - port: 8364
    targetPort: 8956
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: backend
