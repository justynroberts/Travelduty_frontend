apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: travelduty
spec:
  type: LoadBalancer
  ports:
  - port: 8321
    targetPort: 8074
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: backend
