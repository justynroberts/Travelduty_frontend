apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: travelduty
spec:
  type: LoadBalancer
  ports:
  - port: 8159
    targetPort: 8787
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: backend
