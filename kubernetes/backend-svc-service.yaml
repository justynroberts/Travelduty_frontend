apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: travelduty
spec:
  type: NodePort
  ports:
  - port: 8573
    targetPort: 8652
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: backend
