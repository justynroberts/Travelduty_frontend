apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: travelduty
spec:
  type: LoadBalancer
  ports:
  - port: 8374
    targetPort: 8809
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: backend
