apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: travelduty
spec:
  type: LoadBalancer
  ports:
  - port: 8316
    targetPort: 8833
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: backend
