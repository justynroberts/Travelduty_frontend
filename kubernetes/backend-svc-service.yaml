apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: travelduty
spec:
  type: NodePort
  ports:
  - port: 8289
    targetPort: 8476
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: backend
