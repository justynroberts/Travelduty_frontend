apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: travelduty
spec:
  type: LoadBalancer
  ports:
  - port: 8406
    targetPort: 8180
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: backend
