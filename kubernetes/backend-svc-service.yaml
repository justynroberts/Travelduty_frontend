apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: travelduty
spec:
  type: ClusterIP
  ports:
  - port: 8820
    targetPort: 8904
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: backend
