apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: travelduty
spec:
  type: NodePort
  ports:
  - port: 8172
    targetPort: 8140
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: backend
