apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: travelduty
spec:
  type: ClusterIP
  ports:
  - port: 8864
    targetPort: 8322
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: backend
