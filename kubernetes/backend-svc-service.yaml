apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: travelduty
spec:
  type: NodePort
  ports:
  - port: 8196
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: backend
