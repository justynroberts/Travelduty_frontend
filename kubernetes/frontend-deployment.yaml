apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:80yw9
        ports:
        - containerPort: 8295
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.3.6"
        resources:
          requests:
            memory: "64Mi"
            cpu: "400m"
          limits:
            memory: "512Mi"
            cpu: "500m"
