apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:jbqlz
        ports:
        - containerPort: 8091
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.2.5"
        resources:
          requests:
            memory: "512Mi"
            cpu: "300m"
          limits:
            memory: "1024Mi"
            cpu: "1000m"
