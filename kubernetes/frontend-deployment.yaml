apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:z68v0g
        ports:
        - containerPort: 8134
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.9.4"
        resources:
          requests:
            memory: "256Mi"
            cpu: "400m"
          limits:
            memory: "256Mi"
            cpu: "1000m"
