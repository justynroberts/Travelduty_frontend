apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:j0ddnl
        ports:
        - containerPort: 8687
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.4.8"
        resources:
          requests:
            memory: "256Mi"
            cpu: "400m"
          limits:
            memory: "256Mi"
            cpu: "500m"
