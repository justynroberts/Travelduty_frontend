apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:sfjb5x
        ports:
        - containerPort: 8698
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.1.6"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "1500m"
