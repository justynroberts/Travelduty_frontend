apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:8mnbuf
        ports:
        - containerPort: 8852
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.3.9"
        resources:
          requests:
            memory: "512Mi"
            cpu: "400m"
          limits:
            memory: "128Mi"
            cpu: "1500m"
