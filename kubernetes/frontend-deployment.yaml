apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:w6l1e
        ports:
        - containerPort: 8598
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.9.8"
        resources:
          requests:
            memory: "64Mi"
            cpu: "400m"
          limits:
            memory: "256Mi"
            cpu: "1000m"
