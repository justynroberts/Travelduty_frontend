apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:7qe4c
        ports:
        - containerPort: 8750
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.7.2"
        resources:
          requests:
            memory: "512Mi"
            cpu: "400m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
