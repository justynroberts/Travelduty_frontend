apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:ehthw
        ports:
        - containerPort: 8901
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.7.0"
        resources:
          requests:
            memory: "64Mi"
            cpu: "400m"
          limits:
            memory: "512Mi"
            cpu: "1500m"
