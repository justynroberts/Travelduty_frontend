apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:hij5di
        ports:
        - containerPort: 8350
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.1.9"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "1000m"
