apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:gr6ytq
        ports:
        - containerPort: 8690
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.3.6"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "128Mi"
            cpu: "500m"
