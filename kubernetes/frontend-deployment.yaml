apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:q1rkzc
        ports:
        - containerPort: 8969
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.8.3"
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "256Mi"
            cpu: "1500m"
