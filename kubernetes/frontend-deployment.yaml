apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:jiiqv
        ports:
        - containerPort: 8347
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.1.7"
        resources:
          requests:
            memory: "512Mi"
            cpu: "400m"
          limits:
            memory: "128Mi"
            cpu: "1500m"
