apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:u6pm8b
        ports:
        - containerPort: 8626
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.6.5"
        resources:
          requests:
            memory: "128Mi"
            cpu: "200m"
          limits:
            memory: "1024Mi"
            cpu: "1500m"
