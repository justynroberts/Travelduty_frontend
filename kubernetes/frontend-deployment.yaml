apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:ghfqf
        ports:
        - containerPort: 8641
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.8.7"
        resources:
          requests:
            memory: "128Mi"
            cpu: "400m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
