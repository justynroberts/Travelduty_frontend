apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:dr7nfr
        ports:
        - containerPort: 8928
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.5.8"
        resources:
          requests:
            memory: "128Mi"
            cpu: "400m"
          limits:
            memory: "512Mi"
            cpu: "500m"
