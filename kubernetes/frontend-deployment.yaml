apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:5cymip
        ports:
        - containerPort: 8227
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.0.8"
        resources:
          requests:
            memory: "256Mi"
            cpu: "500m"
          limits:
            memory: "128Mi"
            cpu: "1500m"
