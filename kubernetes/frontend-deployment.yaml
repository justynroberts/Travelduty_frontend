apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:nj5kzc
        ports:
        - containerPort: 8935
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.7.4"
        resources:
          requests:
            memory: "512Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "500m"
