apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:xy8ryp
        ports:
        - containerPort: 8279
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.6.7"
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "128Mi"
            cpu: "1000m"
