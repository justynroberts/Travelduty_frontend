apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: travelduty
    component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: frontend
  template:
    metadata:
      labels:
        app: travelduty
        component: frontend
    spec:
      containers:
      - name: frontend
        image: travelduty/frontend:ocrd4
        ports:
        - containerPort: 8509
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.8.7"
        resources:
          requests:
            memory: "512Mi"
            cpu: "400m"
          limits:
            memory: "512Mi"
            cpu: "1500m"
