apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    app: travelduty
    component: api-gateway
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: api-gateway
  template:
    metadata:
      labels:
        app: travelduty
        component: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: travelduty/api-gateway:be2to
        ports:
        - containerPort: 8721
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.6.4"
        resources:
          requests:
            memory: "256Mi"
            cpu: "300m"
          limits:
            memory: "128Mi"
            cpu: "500m"
