apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    app: travelduty
    component: api-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: api-gateway
  template:
    metadata:
      labels:
        app: travelduty
        component: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: travelduty/api-gateway:oqqgnh
        ports:
        - containerPort: 8779
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.1.2"
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1024Mi"
            cpu: "1500m"
