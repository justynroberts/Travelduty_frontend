apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    app: travelduty
    component: api-gateway
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: api-gateway
  template:
    metadata:
      labels:
        app: travelduty
        component: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: travelduty/api-gateway:90q82c
        ports:
        - containerPort: 8437
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.9.6"
        resources:
          requests:
            memory: "512Mi"
            cpu: "400m"
          limits:
            memory: "128Mi"
            cpu: "1000m"
