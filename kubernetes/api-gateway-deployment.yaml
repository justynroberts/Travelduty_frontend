apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    app: travelduty
    component: api-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: api-gateway
  template:
    metadata:
      labels:
        app: travelduty
        component: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: travelduty/api-gateway:exbu6t
        ports:
        - containerPort: 8318
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.5.3"
        resources:
          requests:
            memory: "128Mi"
            cpu: "400m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
