apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    app: travelduty
    component: api-gateway
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: api-gateway
  template:
    metadata:
      labels:
        app: travelduty
        component: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: travelduty/api-gateway:fu430i
        ports:
        - containerPort: 8092
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.4.5"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "256Mi"
            cpu: "1000m"
