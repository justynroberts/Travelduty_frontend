apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    app: travelduty
    component: api-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: api-gateway
  template:
    metadata:
      labels:
        app: travelduty
        component: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: travelduty/api-gateway:1ndrca
        ports:
        - containerPort: 8714
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.6.1"
        resources:
          requests:
            memory: "256Mi"
            cpu: "300m"
          limits:
            memory: "128Mi"
            cpu: "1500m"
