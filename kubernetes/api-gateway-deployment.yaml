apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    app: travelduty
    component: api-gateway
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: api-gateway
  template:
    metadata:
      labels:
        app: travelduty
        component: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: travelduty/api-gateway:0izkj
        ports:
        - containerPort: 8459
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.1.9"
        resources:
          requests:
            memory: "512Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "1500m"
