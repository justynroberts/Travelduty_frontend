apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    app: travelduty
    component: api-gateway
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: api-gateway
  template:
    metadata:
      labels:
        app: travelduty
        component: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: travelduty/api-gateway:iu3fqe
        ports:
        - containerPort: 8298
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.4.5"
        resources:
          requests:
            memory: "128Mi"
            cpu: "400m"
          limits:
            memory: "1024Mi"
            cpu: "500m"
