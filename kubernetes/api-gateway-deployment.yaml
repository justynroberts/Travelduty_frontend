apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    app: travelduty
    component: api-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: api-gateway
  template:
    metadata:
      labels:
        app: travelduty
        component: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: travelduty/api-gateway:yi1xtq
        ports:
        - containerPort: 8575
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.7.9"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "500m"
