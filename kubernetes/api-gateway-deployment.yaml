apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    app: travelduty
    component: api-gateway
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: api-gateway
  template:
    metadata:
      labels:
        app: travelduty
        component: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: travelduty/api-gateway:os2jqg
        ports:
        - containerPort: 8761
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.2.3"
        resources:
          requests:
            memory: "512Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
