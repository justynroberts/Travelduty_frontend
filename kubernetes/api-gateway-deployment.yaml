apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    app: travelduty
    component: api-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: api-gateway
  template:
    metadata:
      labels:
        app: travelduty
        component: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: travelduty/api-gateway:v9gpd
        ports:
        - containerPort: 8991
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.1.0"
        resources:
          requests:
            memory: "128Mi"
            cpu: "400m"
          limits:
            memory: "128Mi"
            cpu: "1000m"
