apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    app: travelduty
    component: api-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: api-gateway
  template:
    metadata:
      labels:
        app: travelduty
        component: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: travelduty/api-gateway:9n3a8
        ports:
        - containerPort: 8128
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.7.7"
        resources:
          requests:
            memory: "64Mi"
            cpu: "400m"
          limits:
            memory: "256Mi"
            cpu: "500m"
