apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    app: travelduty
    component: api-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: api-gateway
  template:
    metadata:
      labels:
        app: travelduty
        component: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: travelduty/api-gateway:ptffj
        ports:
        - containerPort: 8608
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.1.1"
        resources:
          requests:
            memory: "512Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "500m"
