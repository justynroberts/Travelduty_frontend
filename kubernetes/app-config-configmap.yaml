apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  labels:
    app: travelduty
data:
  api_endpoint: https://api.travelduty.com
  log_level: info
  cache_ttl: "1905"
  max_connections: "48"
  timezone: UTC
