apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  labels:
    app: travelduty
data:
  api_endpoint: https://api.travelduty.com
  log_level: info
  cache_ttl: "3697"
  max_connections: "53"
  timezone: UTC
