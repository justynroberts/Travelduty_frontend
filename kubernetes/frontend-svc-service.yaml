apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: LoadBalancer
  ports:
  - port: 8804
    targetPort: 8450
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
