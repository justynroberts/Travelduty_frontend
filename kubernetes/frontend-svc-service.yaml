apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: ClusterIP
  ports:
  - port: 8508
    targetPort: 8045
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
