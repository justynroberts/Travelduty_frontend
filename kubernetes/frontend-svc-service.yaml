apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: NodePort
  ports:
  - port: 8356
    targetPort: 8865
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
