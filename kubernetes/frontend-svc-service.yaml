apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: ClusterIP
  ports:
  - port: 8851
    targetPort: 8094
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
