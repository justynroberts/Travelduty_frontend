apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: ClusterIP
  ports:
  - port: 8232
    targetPort: 8285
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
