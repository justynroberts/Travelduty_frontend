apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: NodePort
  ports:
  - port: 8619
    targetPort: 8359
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
