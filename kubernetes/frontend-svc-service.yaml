apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: NodePort
  ports:
  - port: 8760
    targetPort: 8962
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
