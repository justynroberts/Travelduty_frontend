apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: LoadBalancer
  ports:
  - port: 8158
    targetPort: 8949
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
