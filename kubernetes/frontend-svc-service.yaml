apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: ClusterIP
  ports:
  - port: 8051
    targetPort: 8991
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
