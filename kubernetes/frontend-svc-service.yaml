apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: NodePort
  ports:
  - port: 8534
    targetPort: 8233
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
