apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: NodePort
  ports:
  - port: 8655
    targetPort: 8563
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
