apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: NodePort
  ports:
  - port: 8149
    targetPort: 8484
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
