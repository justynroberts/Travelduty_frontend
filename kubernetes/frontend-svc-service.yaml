apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: NodePort
  ports:
  - port: 8532
    targetPort: 8302
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
