apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: LoadBalancer
  ports:
  - port: 8517
    targetPort: 8731
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
