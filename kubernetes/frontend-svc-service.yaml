apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: NodePort
  ports:
  - port: 8564
    targetPort: 8051
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
