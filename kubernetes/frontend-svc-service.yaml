apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: NodePort
  ports:
  - port: 8859
    targetPort: 8068
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
