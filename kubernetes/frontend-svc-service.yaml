apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: LoadBalancer
  ports:
  - port: 8751
    targetPort: 8035
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
