apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: travelduty
spec:
  type: ClusterIP
  ports:
  - port: 8665
    targetPort: 8214
    protocol: TCP
    name: http
  selector:
    app: travelduty
    component: frontend
