apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:ahxts
        ports:
        - containerPort: 8818
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.6.5"
        resources:
          requests:
            memory: "256Mi"
            cpu: "400m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
