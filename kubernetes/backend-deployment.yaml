apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:42fmkq
        ports:
        - containerPort: 8600
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.6.5"
        resources:
          requests:
            memory: "128Mi"
            cpu: "400m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
