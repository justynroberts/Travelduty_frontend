apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:f11mvr
        ports:
        - containerPort: 8626
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.7.6"
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "128Mi"
            cpu: "1000m"
