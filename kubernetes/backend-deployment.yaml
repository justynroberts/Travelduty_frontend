apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: travelduty
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:4.6.6
        ports:
        - containerPort: 8597
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
