apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:ikzsen
        ports:
        - containerPort: 8566
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.2.3"
        resources:
          requests:
            memory: "64Mi"
            cpu: "300m"
          limits:
            memory: "1024Mi"
            cpu: "500m"
