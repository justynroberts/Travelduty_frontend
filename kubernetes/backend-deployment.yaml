apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:9md1rk
        ports:
        - containerPort: 8776
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.9.6"
        resources:
          requests:
            memory: "64Mi"
            cpu: "300m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
