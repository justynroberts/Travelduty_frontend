apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:vqf8po
        ports:
        - containerPort: 8289
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.4.5"
        resources:
          requests:
            memory: "256Mi"
            cpu: "400m"
          limits:
            memory: "1024Mi"
            cpu: "500m"
