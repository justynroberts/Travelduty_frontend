apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:i4q6l
        ports:
        - containerPort: 8559
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.9.1"
        resources:
          requests:
            memory: "256Mi"
            cpu: "400m"
          limits:
            memory: "128Mi"
            cpu: "500m"
