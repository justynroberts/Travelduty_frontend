apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:q6t3ki
        ports:
        - containerPort: 8582
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.5.9"
        resources:
          requests:
            memory: "512Mi"
            cpu: "300m"
          limits:
            memory: "256Mi"
            cpu: "1500m"
