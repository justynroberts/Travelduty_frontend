apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:kg10x
        ports:
        - containerPort: 8658
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.1.6"
        resources:
          requests:
            memory: "256Mi"
            cpu: "400m"
          limits:
            memory: "256Mi"
            cpu: "500m"
