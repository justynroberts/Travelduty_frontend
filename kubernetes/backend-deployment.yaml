apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:r33hxm
        ports:
        - containerPort: 8311
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.3.4"
        resources:
          requests:
            memory: "512Mi"
            cpu: "400m"
          limits:
            memory: "256Mi"
            cpu: "1000m"
