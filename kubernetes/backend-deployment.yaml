apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:26i6o
        ports:
        - containerPort: 8815
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.1.0"
        resources:
          requests:
            memory: "256Mi"
            cpu: "300m"
          limits:
            memory: "1024Mi"
            cpu: "1500m"
