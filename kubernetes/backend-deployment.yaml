apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:mwehrh
        ports:
        - containerPort: 8605
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.0.5"
        resources:
          requests:
            memory: "64Mi"
            cpu: "400m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
