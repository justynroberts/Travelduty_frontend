apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: travelduty
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:5.1.3
        ports:
        - containerPort: 3703
        resources:
          requests:
            memory: "128Mi"
            cpu: "250m"
