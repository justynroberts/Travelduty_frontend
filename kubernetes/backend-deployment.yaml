apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:x3m4lk
        ports:
        - containerPort: 8027
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.2.4"
        resources:
          requests:
            memory: "64Mi"
            cpu: "300m"
          limits:
            memory: "1024Mi"
            cpu: "1000m"
