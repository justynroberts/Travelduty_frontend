apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:284p8h
        ports:
        - containerPort: 8245
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.6.0"
        resources:
          requests:
            memory: "128Mi"
            cpu: "300m"
          limits:
            memory: "1024Mi"
            cpu: "500m"
