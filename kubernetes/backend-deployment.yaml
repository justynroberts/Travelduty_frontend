apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:vkcgrq
        ports:
        - containerPort: 8634
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.2.7"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "256Mi"
            cpu: "500m"
