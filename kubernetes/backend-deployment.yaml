apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:3n2b0u
        ports:
        - containerPort: 8111
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.1.0"
        resources:
          requests:
            memory: "512Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "500m"
