apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:2j088j
        ports:
        - containerPort: 8211
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.3.1"
        resources:
          requests:
            memory: "64Mi"
            cpu: "400m"
          limits:
            memory: "1024Mi"
            cpu: "1000m"
