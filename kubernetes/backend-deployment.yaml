apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:o039de
        ports:
        - containerPort: 8167
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.4.6"
        resources:
          requests:
            memory: "512Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "1000m"
