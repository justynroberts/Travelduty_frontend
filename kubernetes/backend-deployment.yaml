apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:8aaadf
        ports:
        - containerPort: 8382
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.1.7"
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "128Mi"
            cpu: "500m"
