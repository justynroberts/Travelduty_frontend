apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:upcc7
        ports:
        - containerPort: 8772
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.1.3"
        resources:
          requests:
            memory: "64Mi"
            cpu: "300m"
          limits:
            memory: "512Mi"
            cpu: "1500m"
