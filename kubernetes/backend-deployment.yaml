apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:797miq
        ports:
        - containerPort: 8983
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.9.7"
        resources:
          requests:
            memory: "64Mi"
            cpu: "400m"
          limits:
            memory: "256Mi"
            cpu: "1500m"
