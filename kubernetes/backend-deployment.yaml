apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:hmid8c
        ports:
        - containerPort: 8501
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.2.4"
        resources:
          requests:
            memory: "64Mi"
            cpu: "300m"
          limits:
            memory: "512Mi"
            cpu: "500m"
