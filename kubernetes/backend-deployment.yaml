apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:8utwki
        ports:
        - containerPort: 8912
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.0.3"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "128Mi"
            cpu: "500m"
