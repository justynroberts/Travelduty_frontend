apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: travelduty
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelduty
      component: backend
  template:
    metadata:
      labels:
        app: travelduty
        component: backend
    spec:
      containers:
      - name: backend
        image: travelduty/backend:5cye8n
        ports:
        - containerPort: 8937
        env:
        - name: NODE_ENV
          value: production
        - name: VERSION
          value: "1.3.3"
        resources:
          requests:
            memory: "64Mi"
            cpu: "400m"
          limits:
            memory: "128Mi"
            cpu: "500m"
